{
  "private": "true",
  "scripts": {
    "build": "ts-node generator/index.ts",
    "serve": "sirv public",
    "dev": "concurrently \"nodemon -e js,ts,html,css,md --ignore public --exec npm run build\" \"npm run serve\""
  },
  "devDependencies": {
    "@types/marked": "^2.0.0",
    "@types/node": "^14.14.35",
    "concurrently": "^6.0.0",
    "nodemon": "^2.0.7",
    "prettier": "^2.2.1",
    "sirv-cli": "^1.0.11",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "marked": "^2.0.1"
  }
}
